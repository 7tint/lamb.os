import React, { ReactElement } from "react";

import { Box, Text } from "@chakra-ui/react";

const About = (): ReactElement => {
  return (
    <Box height="450px" width="700px" px={4} py={2}>
      <Text>About</Text>
    </Box>
  );
};

export default About;
