import React, { ReactElement } from "react";

import { Box, Text } from "@chakra-ui/react";

const Computer = (): ReactElement => {
  return (
    <Box>
      <Text>Computer</Text>
    </Box>
  );
};

export default Computer;
